import { BarChart, Bar, XAxis, YAxis, Tooltip, ResponsiveContainer, Legend } from 'recharts';
import ChartContainer from './shared/ChartContainer';
export default function SectionBreakdownBars({ data = [], loading, error, dataTestId, height = 280 }) {return <ChartContainer title="Section Breakdown" summary="Correct, wrong, and unattempted counts." loading={loading} error={error} isEmpty={!data.length} dataTestId={dataTestId} height={height}><ResponsiveContainer width="100%" height={height-40}><BarChart data={data}><XAxis dataKey="section" /><YAxis /><Tooltip /><Legend /><Bar stackId="a" dataKey="correct" fill="#166534" /><Bar stackId="a" dataKey="wrong" fill="#b91c1c" /><Bar stackId="a" dataKey="unattempted" fill="#64748b" /></BarChart></ResponsiveContainer></ChartContainer>;}
