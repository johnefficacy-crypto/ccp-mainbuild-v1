# Reports Components Library

Pure presentational components for Study OS reporting surfaces. No fetching logic.

## Exports
See `index.js` for full list.

## Patterns
- All chart-like components support `loading`, `error`, empty states via `ChartContainer`.
- Accept parent-provided data and callbacks only.
- Use existing theme tokens with fallback colors.
