[pytest]
pythonpath = .
markers =
    integration: tests that require live Supabase/Razorpay/backend network services
    slow: tests that are computationally expensive (OCR, large fixtures)
addopts = -m "not integration"
