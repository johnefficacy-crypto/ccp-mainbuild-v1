# Starter Template Notes

- `starter.csv` is valid against `schema.json`.
- Keep `external_id` unique and stable.
- Use booleans as lowercase `true` or `false`.
- When `is_current=true`, fill both `valid_from` and `valid_until` in ISO8601.
