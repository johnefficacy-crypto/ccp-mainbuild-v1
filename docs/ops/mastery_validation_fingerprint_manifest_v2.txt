# Mastery Shadow Gate — Validation Fingerprint Manifest v2
#
# Supersedes the informal 18-file set used in the PR-6 inspection.
# Added migration 180 (shadow idempotency) and migration 182 (correction
# draft atomic RPCs) which were absent from v1 and are safety-critical.
# Added complete attempt_analytics package (7 files): mock_engine.py calls
# compute_and_persist() which persists mock_attempt_response_classification
# rows that MasteryWriter gates on; classifier/service changes would
# otherwise leave the fingerprint unchanged while changing data under
# validation.
# Added event-input path — backend (3 files): attempt_analytics/service.py
# reads mock_attempt_events; time_analytics.py derives dwell time from
# question-visit events; classifier.py uses that timing.
# Added event-input path — frontend (2 files): MockAttemptShell.jsx
# decides when question.visited is emitted; attemptEventBus.js controls
# timestamps, buffering, flush cadence, loss behavior, and POST payloads.
# Changes to either file alter event data reaching the backend ingest
# path without changing the backend fingerprint.
# Added event-acceptance dependencies (PR #800 review): core/auth.py
# (get_current_user gates whether every client event batch is accepted — a
# change here can silently drop or admit ingestion) and frontend
# lib/supabase.js (the bearer-token source the bus authenticates with). Either
# can alter which events reach the DB without changing any other listed file.
# Added answer-write dependencies (PR #803 review): useAnswerSync.js controls
# debounce/retry/client_seq/flushMany and whether the response row receives
# selected_option_id, is_visited, and time_spent_sec — the scoring/classification
# inputs AND the fallback source when event coverage is incomplete; and
# lib/api.js is the HTTP client MockAttemptShell uses for the answer and submit
# writes (bearer auth, timeout/abort, request construction, error semantics), so
# it too can change whether response/scoring inputs persist. A change to either
# can alter the validation outcome without changing any other listed file.
#
# TRANSITIVE-DEPENDENCY INCLUSION RULE: this manifest is the transitive closure
# of every file that can change the mastery-validation data under observation.
# A file is IN-SCOPE if it can alter, without changing any other listed file,
# any of: (1) which telemetry events are emitted or their timing/payloads;
# (2) whether a client event batch is accepted/ingested (auth, endpoint, schema);
# (3) the scoring / classification / dwell inputs, or the fallback source
# (mock_attempt_responses.* writers). Pure presentation, routing, or styling is
# OUT-OF-SCOPE. Adding/removing a file requires operator approval (below) and a
# recomputed hash + regenerated attestation.
#
# STATUS: FREEZE PENDING — all code/tooling blockers closed; OPERATOR APPROVAL
# the only remaining gate (PR #803 review). The PR #803 checkpost blockers are
# now resolved:
#   [P0 RESOLVED] Submit-time telemetry race — (a) MockAttemptShell.doSubmit()
#        awaits eventBus.flushAndWait() (time-bounded, ACK-gated) BEFORE POSTing
#        /submit; (b) the backend idempotently recomputes analytics on late
#        /events accepted within the grace window (mock_attempt_events.py ->
#        compute_and_persist), and on recompute FAILURE durably schedules the
#        existing analytics_retry job (a duplicate replay is accepted=0 and would
#        not retrigger, so the events being ACKed cannot strand a stale snapshot).
#        The race is fully closed, not merely mitigated. Regression:
#        MockAttemptShell.submitFlush.test.jsx, flushAndWait unit tests, and
#        test_attempt_events recompute + recompute-failure + duplicate-replay tests.
#   [P1 RESOLVED] Boundary closed over the answer-write dependency
#        useAnswerSync.js + lib/api.js (added; 34 -> 36) and a transitive-dependency inclusion
#        rule is documented above.
#   [P1 RESOLVED] Telemetry-quality gate is executable and authoritative:
#        `shadow-analysis telemetry-quality` validates the PERSISTED
#        mock_attempt_summary.analytics_quality snapshot (so late events cannot
#        mask the fallback used at submit), scoped to the EXACT mock_mastery_shadow
#        population. Metrics: fallback_engaged_question_count (answered/marked
#        questions that fell back — untouched excluded), events_used,
#        attempts_missing_snapshot, and trailing_gap_count. Fail-closed: exits
#        non-zero on FAIL and rejects --min-attempts < 1. trailing_gap_count
#        counts MISSING DISTINCT sequences through the declared boundary (the
#        attempt.submit_flush marker is itself seq == declared_final, so the old
#        `declared_final - max(seq)` was always 0; corrected). KNOWN LIMITATION
#        (PR #803 #4): the population is the REALIZED mock_mastery_shadow set, so
#        an attempt that failed before writing any shadow row is invisible to
#        THIS gate — that writer-failure mode is covered by the shadow-replay /
#        readiness gates. Deriving an expected-from-submitted population needs the
#        authoritative effective-mode record (FF/pinned), an operator decision.
#   [P2 RESOLVED] verify_mastery_fingerprint.sh cross-checks the recorded digest
#        across manifest / pr7 / checklist AND requires a pinned SHA (EXPECTED_SHA,
#        or SKIP_SHA=1 for a content-only check).
#   [OPEN — OPERATOR ONLY] PR #800 remains CODE-FIXED / VALIDATION PENDING with
#        its three staging checks unchecked, and the 34 -> 36 manifest expansion
#        is PROPOSED pending operator approval. Per AGENTS.md the gate stays
#        FREEZE PENDING until the operator validates #800 on staging and approves
#        the boundary. NO operator approval is asserted in-repo.
#
# The hash below is therefore a REFERENCE fingerprint at this branch state, NOT
# the window_start hash. The freeze hash must be recomputed (re-pinned to the
# post-merge main SHA) once the operator captures approval.
#
# Reference fingerprint (PR #803 branch, current 36-file boundary):
#   9633b8eb58dc957ef0800857b6d3b4f599471bbd4861a7bb5f685c079efe7b3c
# A per-file SHA-256 attestation is committed alongside this manifest:
#   docs/ops/mastery_validation_fingerprint_manifest_v2.attestation.txt
# Verify (fail-closed) with: bash scripts/verify_mastery_fingerprint.sh
#
# Superseded reference hashes (NOT window_start hashes):
#   b7394b79e00dc320705a4ccb0380afb2b0275f6cf9f0289f07d80e7ba0c3bc2b  (main @ 1679adb8, 32 files, pre-#800)
#   96dd2a67756d7af4837daa68c495c8ebef88b2bb5d1b64bf1206c1720b907a4b  (main @ c9c44a9e, 32 files, bugs present)
#
# To compute the combined fingerprint at any SHA, run from repo root:
#
#   set -euo pipefail
#   readarray -t _files < <(grep -v '^#' docs/ops/mastery_validation_fingerprint_manifest_v2.txt | grep -v '^$')
#   _expected=36
#   _actual=${#_files[@]}
#   [[ $_actual -eq $_expected ]] || { echo "ERROR: expected $_expected files, got $_actual" >&2; exit 1; }
#   for _f in "${_files[@]}"; do
#     [[ -f "$_f" ]] || { echo "ERROR: missing $_f" >&2; exit 1; }
#   done
#   sha256sum "${_files[@]}" | sha256sum
#
# The combined fingerprint must be identical at window_start and window_end.
# Any change to a file in this manifest resets the shadow observation clock.
#
# DO NOT add files to this manifest without operator approval.
# DO NOT remove files from this manifest without operator approval.

app/backend/app/study_os/mastery_writer.py
app/backend/app/study_os/attempt_classification_readiness.py
app/backend/app/study_os/attempt_derivation.py
app/backend/app/study_os/mastery_engine/__init__.py
app/backend/app/study_os/mastery_engine/correction_tasks.py
app/backend/app/study_os/mastery_engine/error_patterns.py
app/backend/app/study_os/mastery_engine/mastery_delta.py
app/backend/app/study_os/mastery_engine/schemas.py
app/backend/app/study_os/mastery_engine/service.py
app/backend/app/study_os/correction_policy.py
app/backend/app/study_os/mock_engine.py
app/backend/app/study_os/mocks.py
app/backend/app/study_os/attempt_analytics/__init__.py
app/backend/app/study_os/attempt_analytics/service.py
app/backend/app/study_os/attempt_analytics/schemas.py
app/backend/app/study_os/attempt_analytics/topic_breakdown.py
app/backend/app/study_os/attempt_analytics/time_analytics.py
app/backend/app/study_os/attempt_analytics/scoring.py
app/backend/app/study_os/attempt_analytics/classifier.py
app/backend/app/study_os/attempt_events.py
app/backend/app/study_os/attempt_event_types.py
app/backend/app/api/mock_engine.py
app/backend/app/api/canonical.py
app/backend/app/api/study_os.py
app/backend/app/api/admin_study_os.py
app/backend/app/api/mock_attempt_events.py
tools/mastery_shadow_analysis/shadow_analysis.py
app/supabase/migrations/180_mock_mastery_shadow_idempotency.sql
app/supabase/migrations/181_mock_correction_tasks_uniqueness.sql
app/supabase/migrations/182_mock_correction_draft_atomic_rpcs.sql
app/frontend/src/pages/study/mocks/MockAttemptShell.jsx
app/frontend/src/pages/study/mocks/attemptEventBus.js
app/backend/app/core/auth.py
app/frontend/src/lib/supabase.js
app/frontend/src/pages/study/mocks/useAnswerSync.js
app/frontend/src/lib/api.js
